一、集合框架
1.集合、数组都是对多个数据进行存储操作的结构，简称java容器
说明：此时的存储，主要是指存层面的存储，不涉及到持久化的存储

2.1数组在存储多个数据封面的特点：
  》一旦初始化后，它的长度就确定了
  》数组一旦定义好后，它的数据类型也就确定了。就只能操作指定类型的数据
  比如：String[] arr; int[] str;

2.2数组在存储多个数据方面的特点
    》一旦初始化以后，它的长度就确定了
    》数组中提供的方法非常有限，对于添加、删除、插入数据等操作，效率不高
    》获取数组中实际元素的个数的需求，数组没有现成的属性或方法可用
    》数组存储数据的特点：有序。可重复。对于无序，不可重复不能满足